---
phase: 12-tool-surface-robustness-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_tool_surface_hints.py
autonomous: true
requirements: [HINT-01, PARSE-01]
must_haves:
  truths:
    - "A test proves every read-only tool advertises readOnlyHint=true + idempotentHint=true + openWorldHint=true on the stdio MCP face"
    - "A test proves the same three hints hold on the HTTP MCP face (_create_http_mcp_server)"
    - "A test proves the REST wiring uses the shared _READ_ONLY annotation with all three hints"
    - "A bad time_from raises an actionable ValueError naming the param and the ISO-8601 / trailing-Z format"
    - "A bad time_to raises an actionable ValueError naming the param and the ISO-8601 / trailing-Z format"
    - "A valid trailing-Z timestamp parses to a tz-aware UTC datetime"
  artifacts:
    - path: "tests/test_tool_surface_hints.py"
      provides: "HINT-01 annotation coverage across stdio/HTTP MCP/REST + PARSE-01 _parse_iso_utc coverage"
      min_lines: 60
  key_links:
    - from: "tests/test_tool_surface_hints.py"
      to: "src/kafka_mcp/adapters/inbound/mcp_stdio.py"
      via: "create_mcp_server(...).list_tools() annotation introspection + _parse_iso_utc import"
      pattern: "create_mcp_server|_parse_iso_utc"
    - from: "tests/test_tool_surface_hints.py"
      to: "src/kafka_mcp/adapters/inbound/rest_api.py"
      via: "_create_http_mcp_server(...).list_tools() + rest_api._READ_ONLY assertion"
      pattern: "_create_http_mcp_server|_READ_ONLY"
---

<objective>
Prove the frozen read-only tool surface advertises the correct annotation hints
on every face (HINT-01), and that search_messages timestamp parsing rejects bad
input with an actionable, param-named error while accepting a trailing-Z UTC
timestamp (PARSE-01).

Purpose: HINT-01 and PARSE-01 are HARDENING requirements — the behavior already
exists in mcp_stdio.py and rest_api.py. This plan adds TEST-ONLY coverage that
locks the behavior; it MUST NOT modify any src/ file.
Output: tests/test_tool_surface_hints.py — a new, self-contained test module.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/kafka_mcp/adapters/inbound/mcp_stdio.py
@src/kafka_mcp/adapters/inbound/rest_api.py
@tests/test_inbound.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: HINT-01 — assert all three hints on every tool across stdio MCP, HTTP MCP, and REST wiring</name>
  <files>tests/test_tool_surface_hints.py</files>
  <action>
Create a new test module tests/test_tool_surface_hints.py (fresh file — do NOT
edit tests/test_inbound.py). Reuse the FakeKafkaClient/MockKafkaClient pattern
from tests/test_inbound.py: define a minimal in-module fake client exposing
list_topics / describe_topic / search_messages / get_message /
consumer_group_lag (mirror the MockKafkaClient shape already in test_inbound.py;
you may import MockKafkaClient from tests.test_inbound if the import resolves,
otherwise inline an equivalent minimal fake). No correlate_messages method is
required for the annotation checks.

Implement HINT-01 (per HINT-01 requirement) as parametrized coverage over the
tool set. Use asyncio.run(server.list_tools()) to introspect ToolAnnotations,
matching the existing introspection pattern in test_inbound.py
(test_mcp_tools_have_read_only_hint).

Assert for EVERY registered tool on each MCP face that annotations is not None
and that readOnlyHint, idempotentHint, and openWorldHint are all True:
  - stdio MCP face: kafka_mcp.adapters.inbound.mcp_stdio.create_mcp_server
  - HTTP MCP face: kafka_mcp.adapters.inbound.rest_api._create_http_mcp_server

For the REST wiring face, assert that the shared module-level annotation
kafka_mcp.adapters.inbound.rest_api._READ_ONLY carries readOnlyHint=True,
idempotentHint=True, openWorldHint=True — this is the single annotation object
attached to every REST-mounted HTTP MCP tool at registration, so asserting it
proves the REST face wiring advertises the hints. Also assert the stdio module's
mcp_stdio._READ_ONLY the same way, so both shared constants are pinned.

Do NOT hardcode a fixed tool-name list as the source of truth for the loop —
derive the tool set from list_tools() so a newly added tool cannot silently skip
the hint check. Keep the annotation assertions themselves independent of tool
count. It is acceptable to also assert that the known frozen tool set
(list_topics, describe_topic, search_messages, get_message, consumer_group_lag,
correlate_messages) is a subset of the discovered tools, to catch accidental
tool removal.

This is assertion-only: do not change _READ_ONLY, ToolAnnotations, or any tool
registration in src/.
  </action>
  <verify>
    <automated>python -m pytest tests/test_tool_surface_hints.py -k "hint or annotation" -q</automated>
  </verify>
  <done>
Running pytest with -k "hint or annotation" selects the new tests and they pass.
Every tool on the stdio MCP and HTTP MCP faces asserts readOnlyHint,
idempotentHint, and openWorldHint all True; both _READ_ONLY constants
(mcp_stdio + rest_api) are asserted to carry the three hints. No src/ file is
modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: PARSE-01 — assert _parse_iso_utc rejects bad time_from/time_to and accepts trailing-Z UTC</name>
  <files>tests/test_tool_surface_hints.py</files>
  <action>
In the same tests/test_tool_surface_hints.py module, add PARSE-01 coverage (per
PARSE-01 requirement) for kafka_mcp.adapters.inbound.mcp_stdio._parse_iso_utc.

Add tests asserting:
  - _parse_iso_utc("not-a-timestamp", "time_from") raises ValueError whose
    message names the param (contains "time_from") AND signals the accepted
    format — assert the message references ISO-8601 and the trailing-Z example
    (match on the concept: check the message contains "time_from" and both an
    ISO-8601 marker and a trailing-Z UTC example such as the "...Z" form the
    function emits). Use pytest.raises(ValueError) and inspect str(exc.value).
  - The same for param "time_to" with a distinct bad value, asserting the
    message contains "time_to".
  - A valid trailing-Z timestamp (e.g. "2026-06-01T00:00:00Z") parses to a
    datetime whose tzinfo is not None and whose utcoffset() equals a zero
    timedelta (tz-aware UTC). Assert the parsed value equals the expected
    tz-aware UTC datetime.
  - A naive timestamp without offset (e.g. "2026-06-01T00:00:00") is defaulted
    to UTC (tzinfo is not None, zero utcoffset) — this pins the naive→UTC guard.

Assertion-only: import and call the existing _parse_iso_utc; do NOT modify it.
Do not place the negatively-grepped literal error text inside any comment that
would echo into the written file — assert on substrings via the concept
(param name present, ISO-8601 present, trailing-Z example present).
  </action>
  <verify>
    <automated>python -m pytest tests/test_tool_surface_hints.py -k "parse_iso or timestamp" -q</automated>
  </verify>
  <done>
Bad time_from and time_to each raise a ValueError naming the param and the
accepted ISO-8601 / trailing-Z format; a valid trailing-Z timestamp parses to a
tz-aware UTC datetime; a naive timestamp defaults to UTC. Tests pass and no src/
file is modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → src imports | Tests import and introspect production adapters in-process; no external input crosses a real boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | Test accidentally mutates shared _READ_ONLY / tool registration in src | accept | Tasks are assertion-only by construction; verify step confirms no src/ change. Low risk, no runtime exposure. |
| T-12-02 | Information Disclosure | Test data (fake broker) leaks secrets | accept | Fake client returns deterministic non-sensitive data only; no env/secret access. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies introduced; tests use existing pytest + installed mcp/fastapi. No install tasks, so no legitimacy gate needed. |
</threat_model>

<verification>
- `python -m pytest tests/test_tool_surface_hints.py -q` passes.
- `python -m pytest tests/ -k "annotation or hint" -q` passes (SC-1 selector).
- `python -m pytest tests/ -k "parse_iso or timestamp" -q` passes (SC-2 selector).
- `git diff --name-only src/` shows no source changes.
</verification>

<success_criteria>
- HINT-01: every tool on stdio MCP + HTTP MCP asserts all three hints; both
  _READ_ONLY constants asserted for the REST/stdio wiring.
- PARSE-01: bad time_from/time_to raise actionable param-named ValueErrors;
  trailing-Z parses to tz-aware UTC; naive defaults to UTC.
- No src/ behavior modified.
</success_criteria>

<output>
Create `.planning/phases/12-tool-surface-robustness-coverage/12-01-SUMMARY.md` when done
</output>
