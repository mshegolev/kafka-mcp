---
phase: 07-release-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - glama.json
  - server.json
autonomous: true
requirements: [REL-02]

must_haves:
  truths:
    - "glama.json lists all five tools including consumer_group_lag"
    - "server.json version reflects 1.1.0"
    - "server.json already has HTTP transport in remotes (from Phase 4); no structural changes needed"
    - "glama.json already has HTTP transport in serverConfigOptions (from Phase 4); no structural changes needed"
  artifacts:
    - path: "glama.json"
      provides: "Glama MCP server registry metadata for v1.1"
      contains: "consumer_group_lag"
    - path: "server.json"
      provides: "MCP server.json discovery metadata for v1.1"
      contains: "1.1.0"
  key_links:
    - from: "glama.json"
      to: "src/kafka_mcp/adapters/inbound/mcp_stdio.py"
      via: "tool names must match registered MCP tools"
      pattern: "consumer_group_lag"
    - from: "server.json"
      to: "pyproject.toml"
      via: "version field should be consistent"
      pattern: "1.1.0"
---

<objective>
Update glama.json and server.json to accurately reflect the v1.1 artifact: add the
consumer_group_lag tool entry and bump version to 1.1.0.

Purpose: Ensure the in-repo MCP discovery metadata matches the actual server capabilities
so Glama submission and MCP client discovery work correctly for the v1.1 release.

Output: Updated `glama.json` and `server.json`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@glama.json
@server.json

Current state of both files:
- server.json v0.1.0 — already has HTTP transport in `remotes` array (added Phase 4)
- glama.json — already has HTTP transport in `serverConfigOptions` (added Phase 4)
- Neither file lists `consumer_group_lag` tool (added in Phase 5)
- Version in both files is 0.1.0, needs bump to 1.1.0

The consumer_group_lag tool description from the codebase:
"Report per-partition consumer lag (committed offset vs end offset) for a given
consumer group. Read-only — no commits, no group joins."
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update glama.json for v1.1</name>
  <files>glama.json</files>
  <action>
Update `glama.json` with the following changes:

1. **Add consumer_group_lag to the tools array** — append after the existing
   `get_message` entry:
   ```json
   {
     "name": "consumer_group_lag",
     "description": "Report per-partition consumer lag (committed offset vs end offset) for a given consumer group. Read-only — no commits, no group joins.",
     "readOnlyHint": true
   }
   ```

2. **No other changes needed** — the file already has:
   - Correct `serverConfig` (stdio default)
   - Correct `serverConfigOptions` (HTTP streamable-http, added Phase 4)
   - Correct description, tags, category, vendor, license

Do NOT change the version field in glama.json — Glama schema does not have a top-level
version field (it reads version from PyPI/GitHub).
  </action>
  <verify>
    <automated>python3 -c "
import json, sys
with open('glama.json') as f:
    data = json.load(f)
tools = [t['name'] for t in data['tools']]
expected = ['list_topics', 'describe_topic', 'search_messages', 'get_message', 'consumer_group_lag']
assert tools == expected, f'Expected {expected}, got {tools}'
assert any(t.get('readOnlyHint') for t in data['tools'] if t['name'] == 'consumer_group_lag'), 'Missing readOnlyHint'
assert 'serverConfigOptions' in data, 'Missing serverConfigOptions (HTTP transport)'
print('PASS: glama.json has all 5 tools + HTTP transport + readOnlyHint')
"</automated>
  </verify>
  <done>
    glama.json tools array contains all five tools (list_topics, describe_topic,
    search_messages, get_message, consumer_group_lag) with readOnlyHint: true;
    serverConfigOptions present for HTTP transport
  </done>
</task>

<task type="auto">
  <name>Task 2: Update server.json version to 1.1.0</name>
  <files>server.json</files>
  <action>
Update `server.json` with the following changes:

1. **Bump top-level `version`** from `"0.1.0"` to `"1.1.0"`

2. **Bump package version** in `packages[0].version` from `"0.1.0"` to `"1.1.0"`

3. **No other changes needed** — the file already has:
   - Correct `remotes` array with streamable-http entry (added Phase 4)
   - Correct environment variables for both stdio and HTTP transports
   - Tool list is NOT part of server.json schema (tools are in glama.json)

Note: Do NOT update pyproject.toml version here — that is a release-time action
documented in RELEASE.md. The server.json version is updated to reflect the v1.1
target so the metadata is ready when the release is cut.
  </action>
  <verify>
    <automated>python3 -c "
import json, sys
with open('server.json') as f:
    data = json.load(f)
assert data['version'] == '1.1.0', f'Top-level version: {data[\"version\"]}'
assert data['packages'][0]['version'] == '1.1.0', f'Package version: {data[\"packages\"][0][\"version\"]}'
assert 'remotes' in data, 'Missing remotes (HTTP transport)'
assert any(r['type'] == 'streamable-http' for r in data['remotes']), 'Missing streamable-http remote'
print('PASS: server.json version=1.1.0, has HTTP transport in remotes')
"</automated>
  </verify>
  <done>
    server.json top-level version and packages[0].version are both "1.1.0";
    remotes array contains streamable-http entry for HTTP transport
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| glama.json → Glama registry | Metadata read by external service during submission |
| server.json → MCP clients | Discovery metadata read by MCP client implementations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | glama.json tool list | mitigate | Tool names and descriptions match actual registered MCP tools in mcp_stdio.py; verify via automated test |
| T-07-07 | Info Disclosure | server.json env vars | accept | Environment variable names are not secrets; isSecret=true on password fields already present |
| T-07-08 | Spoofing | version mismatch | mitigate | Automated verify checks version consistency; RELEASE.md documents version bump process |
</threat_model>

<verification>
1. `glama.json` has exactly 5 tools: list_topics, describe_topic, search_messages, get_message, consumer_group_lag
2. All tools in glama.json have `readOnlyHint: true`
3. `glama.json` has `serverConfigOptions` with streamable-http entry
4. `server.json` version is "1.1.0" (top-level and packages[0])
5. `server.json` has `remotes` array with streamable-http entry
6. Both files are valid JSON
</verification>

<success_criteria>
- glama.json accurately reflects the v1.1 tool list (5 tools) and both transport options
- server.json version is 1.1.0 with HTTP transport declared in remotes
- Both files are valid JSON and pass automated verification
</success_criteria>

<output>
After completion, create `.planning/phases/07-release-pipeline/07-02-SUMMARY.md`
</output>
