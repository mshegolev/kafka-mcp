---
phase: "02-search-decode"
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/kafka_mcp/domain/models.py
  - src/kafka_mcp/domain/errors.py
  - src/kafka_mcp/ports/consumer.py
  - src/kafka_mcp/ports/schema_registry.py
  - tests/test_domain.py
autonomous: true
requirements:
  - KAFKA-02
  - KAFKA-03
  - KAFKA-05

must_haves:
  truths:
    - "KafkaMessage is a pydantic v2 model with fields: topic, partition, offset, key, headers, value, timestamp_utc, raw"
    - "KafkaMessage carries Evidence fields: source='kafka', event_type='kafka_message', and keys dict with order_id/msisdn/customer_id/product_id"
    - "DecodeError and MessageNotFoundError are typed domain errors with no I/O imports"
    - "ConsumerPort has fetch_messages() and fetch_message() abstract methods"
    - "SchemaRegistryPort has decode() method returning dict | None"
    - "domain/ and ports/ layers import zero I/O or decode libraries (hexagonal boundary)"
  artifacts:
    - path: "src/kafka_mcp/domain/models.py"
      provides: "KafkaMessage pydantic v2 model"
      contains: "class KafkaMessage"
    - path: "src/kafka_mcp/domain/errors.py"
      provides: "DecodeError and MessageNotFoundError typed exceptions"
      contains: "class DecodeError"
    - path: "src/kafka_mcp/ports/consumer.py"
      provides: "Extended ConsumerPort with fetch_messages + fetch_message"
      contains: "fetch_messages"
    - path: "src/kafka_mcp/ports/schema_registry.py"
      provides: "Extended SchemaRegistryPort with decode method"
      contains: "decode"
  key_links:
    - from: "src/kafka_mcp/domain/models.py"
      to: "pydantic.BaseModel"
      via: "class KafkaMessage(BaseModel)"
      pattern: "class KafkaMessage"
    - from: "src/kafka_mcp/ports/consumer.py"
      to: "src/kafka_mcp/domain/models.py"
      via: "KafkaMessage in return type annotations"
      pattern: "KafkaMessage"
---

<objective>
Phase 2 domain contracts: add KafkaMessage pydantic model, DecodeError,
MessageNotFoundError, and extend ConsumerPort + SchemaRegistryPort with
the method signatures required for search and decode.

Purpose: Establish the exact data shapes and port contracts that all Phase 2
adapters and the domain service build against. Interface-first task ordering:
adapters in plans 02-02 and 02-03 implement against these contracts.

Output: Updated domain/models.py, domain/errors.py, ports/consumer.py,
ports/schema_registry.py with full Phase 2 contracts; 25+ new TDD tests.

Phase goal (per D-02 / ROADMAP Phase 2):
**As an** investigator, **I want to** find Kafka events by key and retrieve
decoded message bodies, **so that** I can build incident timelines from
broker data without needing to know wire formats.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-search-decode/02-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@src/kafka_mcp/domain/models.py
@src/kafka_mcp/domain/errors.py
@src/kafka_mcp/ports/consumer.py
@src/kafka_mcp/ports/schema_registry.py
@tests/test_domain.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: KafkaMessage model + DecodeError + MessageNotFoundError (RED → GREEN)</name>
  <files>
    src/kafka_mcp/domain/models.py,
    src/kafka_mcp/domain/errors.py,
    tests/test_domain.py
  </files>

  <read_first>
    - src/kafka_mcp/domain/models.py — existing TopicInfo/PartitionInfo BaseModel patterns to follow
    - src/kafka_mcp/domain/errors.py — existing ConfigError/TopicNotFoundError patterns; both carry context attributes
    - .planning/phases/02-search-decode/02-CONTEXT.md — KafkaMessage/Evidence Contract section (locked decisions D-67–D-75)
    - .planning/PROJECT.md — Evidence shape: source="kafka", event_type="kafka_message", keys{order_id, msisdn, customer_id, product_id}
    - tests/test_domain.py — existing test structure and MockConsumer (get_partition_ids must remain)
  </read_first>

  <behavior>
    RED phase — write these tests first, run pytest to confirm ALL fail:
    - test_kafka_message_fields: KafkaMessage instance has topic, partition, offset, key, headers, value, timestamp_utc, raw
    - test_kafka_message_key_optional: key=None is valid
    - test_kafka_message_value_optional: value=None is valid (resilient-decode-in-search path per CONTEXT.md)
    - test_kafka_message_raw_bytes: raw field accepts bytes
    - test_kafka_message_timestamp_utc_is_datetime: timestamp_utc field is datetime type
    - test_kafka_message_evidence_source: source == "kafka" (constant Evidence field)
    - test_kafka_message_evidence_event_type: event_type == "kafka_message" (constant Evidence field)
    - test_kafka_message_evidence_keys_present: keys dict contains order_id/msisdn/customer_id/product_id keys (values may be None)
    - test_decode_error_is_domain_exception: DecodeError is subclass of Exception (not ValueError)
    - test_decode_error_carries_topic_partition_offset: DecodeError has .topic, .partition, .offset attributes
    - test_decode_error_carries_reason: DecodeError has .reason: str attribute
    - test_message_not_found_error_carries_coordinates: MessageNotFoundError has .topic, .partition, .offset attributes
    - test_hexagonal_boundary_domain_models: grep -rn "import confluent_kafka\|import fastavro\|import avro\|import google.protobuf" src/kafka_mcp/domain/ returns empty

    GREEN phase — implement:
    KafkaMessage fields (per CONTEXT.md KafkaMessage/Evidence Contract):
    - topic: str
    - partition: int
    - offset: int
    - key: str | None
    - headers: dict[str, str] (default empty dict)
    - value: dict | None
    - timestamp_utc: datetime (use datetime from Python stdlib; must be timezone-aware UTC)
    - raw: bytes
    - source: str = "kafka" (Evidence constant)
    - event_type: str = "kafka_message" (Evidence constant)
    - keys: dict[str, str | None] — default_factory generates {order_id: None, msisdn: None, customer_id: None, product_id: None}

    DecodeError(Exception):
    - __init__(self, topic: str, partition: int, offset: int, reason: str)
    - .topic, .partition, .offset, .reason attributes
    - message: f"Decode failed for {topic}[{partition}]@{offset}: {reason}"

    MessageNotFoundError(Exception):
    - __init__(self, topic: str, partition: int, offset: int)
    - .topic, .partition, .offset attributes
    - message: f"No message at {topic}[{partition}]@{offset}"

    Do NOT import datetime as "from datetime import datetime" — use "from __future__ import annotations"
    and import as "from datetime import datetime, timezone" (stdlib only, no I/O).
    All new tests must pass. Existing 20 domain tests must continue to pass.
  </behavior>

  <action>
    RED: Extend tests/test_domain.py with 13 new tests listed in behavior section.
    Run `pytest tests/test_domain.py -v` — all 13 new tests must FAIL (RED gate).
    Commit: test(02-01): add failing tests for KafkaMessage and new domain errors

    GREEN: Extend src/kafka_mcp/domain/models.py — append KafkaMessage class below TopicInfo.
    Import from datetime: datetime, timezone. Use pydantic Field(default_factory=...) for keys dict
    and headers dict. source and event_type are ClassVar-free literal defaults (pydantic will
    include them in schema). Per CONTEXT.md: raw: bytes stored as-is in domain; inbound faces
    do base64 encoding — NOT in the model itself.

    Extend src/kafka_mcp/domain/errors.py — append DecodeError and MessageNotFoundError.
    Both inherit from Exception (not ValueError). DecodeError stores topic, partition, offset,
    reason as attributes for structured error handling in plan 02-04 service layer.

    Run `pytest tests/test_domain.py -v` — ALL 33+ tests must pass (GREEN gate).
    Hexagonal boundary must hold: grep -rn "import confluent_kafka\|import fastavro\|
    import avro\|import google.protobuf" src/kafka_mcp/domain/ must return empty.
    Commit: feat(02-01): add KafkaMessage model + DecodeError + MessageNotFoundError
  </action>

  <verify>
    <automated>
      pytest tests/test_domain.py -v
    </automated>
  </verify>

  <acceptance_criteria>
    - src/kafka_mcp/domain/models.py defines class KafkaMessage(BaseModel) with fields: topic, partition, offset, key, headers, value, timestamp_utc, raw, source, event_type, keys
    - KafkaMessage.source default is "kafka", KafkaMessage.event_type default is "kafka_message"
    - KafkaMessage.keys default is dict with keys: order_id, msisdn, customer_id, product_id (all None)
    - src/kafka_mcp/domain/errors.py defines DecodeError with .topic/.partition/.offset/.reason attributes
    - src/kafka_mcp/domain/errors.py defines MessageNotFoundError with .topic/.partition/.offset attributes
    - grep -rn "import confluent_kafka\|import fastavro\|import avro\|import google.protobuf" src/kafka_mcp/domain/ returns nothing (hexagonal boundary holds)
    - pytest tests/test_domain.py exits 0 with 33+ tests passing
    - RED commit exists before GREEN commit in git log
  </acceptance_criteria>

  <done>
    All 13 new domain tests pass; all pre-existing domain tests still pass;
    hexagonal boundary grep returns empty for domain/; RED commit precedes GREEN commit.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Extend ConsumerPort and SchemaRegistryPort with Phase 2 method signatures (RED → GREEN)</name>
  <files>
    src/kafka_mcp/ports/consumer.py,
    src/kafka_mcp/ports/schema_registry.py,
    tests/test_domain.py
  </files>

  <read_first>
    - src/kafka_mcp/ports/consumer.py — existing ConsumerPort Protocol with list_topics, get_watermark_offsets, get_partition_ids; runtime_checkable Protocol pattern
    - src/kafka_mcp/ports/schema_registry.py — existing SchemaRegistryPort Protocol with get_schema()
    - .planning/phases/02-search-decode/02-CONTEXT.md — Search Strategy and Decode Pipeline sections
    - .planning/phases/01-foundation/01-03-SUMMARY.md — deviation #3: adding get_partition_ids broke MockConsumer isinstance check; same risk for new methods
    - tests/test_domain.py — MockConsumer definition (must be extended with stub implementations of new methods)
  </read_first>

  <behavior>
    RED phase — write tests first:
    - test_consumer_port_has_fetch_messages: ConsumerPort has method fetch_messages
    - test_consumer_port_has_fetch_message: ConsumerPort has method fetch_message
    - test_consumer_port_mock_implements_protocol: MockConsumer (extended) satisfies isinstance(obj, ConsumerPort)
    - test_schema_registry_port_has_decode: SchemaRegistryPort has method decode
    - test_schema_registry_port_mock_implements: MockSchemaRegistry satisfies isinstance(obj, SchemaRegistryPort)

    GREEN phase:
    ConsumerPort — add two new methods to the Protocol:

    fetch_messages(
        topic: str,
        partition: int,
        start_offset: int,
        stop_offset: int,
        time_to: datetime | None,
        limit: int,
    ) -> list[KafkaMessage]
    Docstring: Consume messages from [start_offset, stop_offset) bounded by time_to and limit.
    Returns KafkaMessage objects with raw bytes; decode is NOT performed here.
    Never commits offsets (assign-based, KAFKA-06).

    fetch_message(
        topic: str,
        partition: int,
        offset: int,
    ) -> KafkaMessage
    Docstring: Fetch a single raw message by exact offset.
    Raises MessageNotFoundError when offset is beyond watermarks.
    Returns KafkaMessage with raw bytes; decode is NOT performed here.

    SchemaRegistryPort — add one new method:
    decode(raw: bytes) -> dict | None
    Docstring: Decode a raw wire-format message payload to a plain dict.
    Returns None when the payload cannot be decoded (resilient path for search_messages).
    Raises DecodeError (for get_message path — callers choose whether to propagate).
    Confluent magic-byte framing (0x00 + 4-byte schema_id) handled in the implementation;
    json.loads fallback when framing absent.

    Important: ConsumerPort imports KafkaMessage and MessageNotFoundError from domain
    — these are pure data types, not I/O. SchemaRegistryPort imports DecodeError and
    dict type only. Both ports remain I/O-library-free.

    Update MockConsumer and MockSchemaRegistry in tests/test_domain.py to implement
    the new methods as stubs (return [] / raise NotImplementedError stubs as needed
    to satisfy isinstance checks). Must not break existing 33+ tests.
  </behavior>

  <action>
    RED: Add 5 new tests to tests/test_domain.py (port contract tests listed in behavior).
    Run `pytest tests/test_domain.py -v` — 5 new tests must FAIL.
    Commit: test(02-01): add failing tests for extended port protocols

    GREEN: Add fetch_messages and fetch_message to ConsumerPort Protocol.
    Add decode to SchemaRegistryPort Protocol.
    Update MockConsumer in tests/test_domain.py with stub implementations:
    fetch_messages returns [], fetch_message raises MessageNotFoundError.
    Add MockSchemaRegistry to tests/test_domain.py with stub decode that returns None.
    Imports in ports/consumer.py: add KafkaMessage from domain.models,
    MessageNotFoundError from domain.errors, datetime from datetime stdlib.
    Imports in ports/schema_registry.py: add DecodeError from domain.errors.
    All imports in port files must be from kafka_mcp.domain.* or Python stdlib only
    — no confluent_kafka, no fastavro, no httpx.

    Run `pytest tests/test_domain.py -v` — ALL tests (38+) must pass.
    Run `grep -rn "import confluent_kafka\|import httpx\|import fastavro"
    src/kafka_mcp/ports/` — must return empty.
    Commit: feat(02-01): extend ConsumerPort and SchemaRegistryPort with Phase 2 methods
  </action>

  <verify>
    <automated>
      pytest tests/test_domain.py -v && grep -c "fetch_messages" src/kafka_mcp/ports/consumer.py
    </automated>
  </verify>

  <acceptance_criteria>
    - src/kafka_mcp/ports/consumer.py declares fetch_messages(topic, partition, start_offset, stop_offset, time_to, limit) -> list[KafkaMessage]
    - src/kafka_mcp/ports/consumer.py declares fetch_message(topic, partition, offset) -> KafkaMessage
    - src/kafka_mcp/ports/schema_registry.py declares decode(raw: bytes) -> dict | None
    - grep -rn "import confluent_kafka\|import httpx\|import fastavro\|import avro\|import google.protobuf" src/kafka_mcp/ports/ returns nothing
    - pytest tests/test_domain.py exits 0 with 38+ tests passing
    - MockConsumer in test_domain.py satisfies isinstance(obj, ConsumerPort) check with all new methods present
  </acceptance_criteria>

  <done>
    ConsumerPort and SchemaRegistryPort carry complete Phase 2 method signatures;
    all port boundary checks pass; isinstance checks for Protocols pass with updated mocks;
    all 38+ domain/port tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain/ → ports/ | Domain imports port protocols; ports import domain types — both must stay I/O-library-free |
| tests → domain/ports | Test file imports domain/port types; test stubs must satisfy Protocol isinstance checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-A | Information Disclosure | KafkaMessage.raw | mitigate | raw stored as bytes in domain; base64 encoding ONLY in inbound adapters (rest_api, mcp_stdio, cli) — never in the model itself; plan 02-05 enforces this at face layer |
| T-02-01-B | Information Disclosure | domain/models.py | mitigate | No SecretStr needed in KafkaMessage (no credentials); timestamp_utc must be UTC-aware to prevent clock-skew timeline errors per PROJECT.md pitfall note |
| T-02-01-C | Tampering | Hexagonal boundary | mitigate | Boundary grep assertion extended to domain/models.py and domain/errors.py — no I/O or decode libs; test_hexagonal_boundary_domain_models asserts this in TDD RED step |
| T-02-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — only stdlib datetime and pydantic (already in pyproject.toml) |
</threat_model>

<verification>
Run all tests after both tasks complete:

```
pytest tests/ -v
```

Expected: 90+ tests pass (78 Phase 1 + new Phase 2 domain/port tests).

Hexagonal boundary check (must return empty output):
```
grep -rn \
  "import confluent_kafka\|import fastavro\|import avro\|import google.protobuf\|import httpx" \
  src/kafka_mcp/domain/ src/kafka_mcp/ports/
```

Port extension check:
```
grep -c "fetch_messages\|fetch_message" src/kafka_mcp/ports/consumer.py
grep -c "decode" src/kafka_mcp/ports/schema_registry.py
```
Both must return >= 1.
</verification>

<success_criteria>
- KafkaMessage pydantic v2 model present with all Investigator Contract fields
- Evidence fields (source, event_type, keys dict) present on KafkaMessage with correct defaults
- DecodeError carries topic/partition/offset/reason; MessageNotFoundError carries topic/partition/offset
- ConsumerPort extended with fetch_messages + fetch_message signatures
- SchemaRegistryPort extended with decode signature
- Zero I/O or decode-library imports in domain/ or ports/ layers
- All pre-existing 78 Phase 1 tests still pass
- TDD RED → GREEN commits present in git log for each task
</success_criteria>

<output>
Create `.planning/phases/02-search-decode/02-01-SUMMARY.md` when done.
</output>
