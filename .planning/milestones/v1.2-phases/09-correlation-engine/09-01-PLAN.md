# Phase 9 Plan: Correlation Engine

## Goal
An investigator can extract correlated entity IDs from search results and follow those IDs into additional topics to build a cross-service event chain — all within a single `correlate_messages` call that returns Evidence-shaped results linked by a correlation chain.

## Success Criteria
1. `KafkaClient.correlate_messages(key="order-123", topics=["orders"], follow_topics=["payments", "shipments"])` extracts correlated IDs (e.g. `trace_id`, `payment_id`, `shipment_id`) from the initial search results by scanning message values and headers for configurable ID field patterns, then searches for those IDs in `follow_topics` — verifiable via `pytest tests/ -k "correlate"` with mocked consumer data containing cross-references
2. The correlation output is a list of messages sorted by `timestamp_utc` across all topics (initial + follow), where each message carries `source="kafka"`, `event_type="correlated_message"`, `timestamp_utc`, and `keys` fields conforming to the Investigator-Contract Evidence shape — verifiable via `pytest tests/ -k "evidence_shape"`
3. Each message in the correlation output includes a `correlation_chain` field that records the ID path that discovered it (e.g. `[{"field": "order_id", "value": "order-123", "hop": 0}, {"field": "trace_id", "value": "abc-123", "hop": 1}]`), so an investigator can reconstruct *why* each message was included — verifiable via `pytest tests/ -k "correlation_chain"`
4. `correlate_messages` reuses the Phase 8 multi-topic search + header filtering internally (no duplicated scan logic); the correlation layer is additive on top of the search domain — verifiable by inspecting that `correlate_messages` delegates to `search_messages` in the implementation and via `pytest tests/ -k "correlate" -v` showing the call chain

## Implementation Tasks

### 1. Domain Layer Changes
- **Create `CorrelationService`** in `src/kafka_mcp/domain/correlation_service.py`:
  - Implement `_extract_correlation_ids` helper function to extract IDs from message values and headers
  - Implement `correlate_messages` method that orchestrates the correlation workflow
  - Reuse existing `TopicService` for multi-topic search operations
  - Build correlation_chain for tracking ID discovery paths
  - Sort final results by `timestamp_utc` across all topics

- **Extend `KafkaMessage` model** in `src/kafka_mcp/domain/models.py`:
  - Add `correlation_chain` field to track the ID path that discovered each message

### 2. Library Adapter Changes
- **Add `correlate_messages` method** in `src/kafka_mcp/adapters/inbound/lib.py`:
  - Add method that accepts `initial_results`, `follow_topics`, and `limit` parameters
  - Initialize and delegate to `CorrelationService`
  - Update docstring to document the new method

### 3. CLI Changes
- **Add `correlate-messages` subcommand** in `src/kafka_mcp/adapters/inbound/cli.py`:
  - Add new subcommand with appropriate arguments (`--key`, `--initial-topics`, `--follow-topics`, `--limit`, etc.)
  - Add help text explaining the correlation functionality
  - Implement `run_correlate_messages` function to handle the CLI workflow
  - Add output formatting for correlation_chain field

### 4. REST API Changes
- **Add `/tools/correlate_messages` endpoint** in `src/kafka_mcp/adapters/inbound/rest_api.py`:
  - Add `CorrelateMessagesRequest` Pydantic model for request validation
  - Add endpoint that accepts `initial_results`, `follow_topics`, and `limit` parameters
  - Implement response formatting consistent with other tools
  - Add appropriate error handling and validation

### 5. MCP Changes
- **Register `correlate_messages` tool** in `src/kafka_mcp/server.py`:
  - Add tool registration with appropriate schema
  - Ensure streamable-HTTP transport is used for large result sets
  - Maintain consistency with other MCP tools

### 6. Testing
- **Add unit tests for `CorrelationService`** in `tests/test_correlation.py`:
  - Test ID extraction from various message formats
  - Test correlation workflow with mocked data
  - Test edge cases (empty results, no correlation IDs, etc.)
  - Test correlation_chain building and sorting

- **Add integration tests** in `tests/test_correlation_integration.py`:
  - Test CLI `correlate-messages` command
  - Test REST API `/tools/correlate_messages` endpoint
  - Test library-level `correlate_messages` method
  - Verify Evidence-shaped output format
  - Test backward compatibility with existing functionality

## Implementation Approach

### Core Algorithm
1. Extract correlation IDs from initial search results by scanning message values and headers
2. For each extracted ID, search follow_topics using intelligent field matching:
   - Search in message keys first
   - Search in common header field names (trace_id, correlation_id, etc.)
   - Search in common value field names (trace_id, correlation_id, etc.)
3. Build correlation_chain for each matched message to track discovery path
4. Combine all results and sort by timestamp_utc
5. Apply limit to prevent unbounded result sets

### ID Extraction Strategy
- Look for common correlation field names in both message values and headers
- Support multiple naming conventions (snake_case, camelCase, kebab-case)
- Include evidence keys in ID extraction
- Return unique set of correlation IDs

### Field Search Strategy
- Search in message keys (`key_field="key"`)
- Search in headers using common correlation field names (`key_field="header:trace_id"`)
- Search in values using common correlation field names (`key_field="value:trace_id"`)

## Dependencies
- Depends on Phase 8 (Multi-Topic Search & Header Filtering)
- Builds on existing `TopicService` and search infrastructure
- Reuses existing MCP, REST, and CLI frameworks

## Backward Compatibility
- No breaking changes to existing functionality
- New methods and endpoints are additive
- Existing tests continue to pass

## Performance Considerations
- Efficient duplicate detection using message coordinates
- Limit enforcement to prevent unbounded result sets
- Single-hop correlation as specified (no recursive depth)
- Reuse of existing optimized search paths

## Verification
- All new correlation tests pass (`pytest tests/ -k "correlate"`)
- Evidence shape compliance verified (`pytest tests/ -k "evidence_shape"`)
- Correlation chain tracking verified (`pytest tests/ -k "correlation_chain"`)
- Existing tests continue to pass (no regressions)