# Phase 10 Plan: 4-Face Symmetry & Integration Tests

## Goal
All v1.2 capabilities (multi-topic search, header filtering, correlate_messages) are accessible identically through MCP stdio, FastAPI REST, and CLI faces — and the integration test suite covers cross-topic scenarios against a real broker.

## Success Criteria
1. The MCP stdio face exposes multi-topic search (`topics` param), header filtering (`headers` param) on the `search_messages` tool, and a new `correlate_messages` tool — all returning the same JSON schema as the lib face; verifiable via `pytest tests/ -k "mcp" -v` exercising the MCP adapter with the new parameters and tool
2. The FastAPI REST face exposes `/tools/search_messages` accepting `topics` + `headers` body fields, and a new `/tools/correlate_messages` endpoint — response schemas match the lib face; verifiable via `pytest tests/ -k "rest" -v` or `pytest tests/ -k "fastapi" -v`
3. The CLI face (`kafka-mcp search-messages --topics ... --headers ...` and `kafka-mcp correlate-messages ...`) produces the same output as the lib face for equivalent inputs; verifiable via `pytest tests/ -k "cli" -v`
4. The existing 323 tests pass without modification alongside the new v1.2 tests — verifiable via `pytest tests/ --tb=short` showing zero regressions and total count ≥ 323
5. Integration tests (`pytest -m integration`) include at least one cross-topic search scenario and one correlation scenario against the real testcontainers broker — verifiable via `pytest -m integration -k "multi_topic or correlate" -v`

## Implementation Tasks

### 1. REST API Changes
- **Update SearchMessagesRequest model** in `src/kafka_mcp/adapters/inbound/rest_api.py`:
  - Add `headers: dict[str, str] | None = None` field to the Pydantic model
  - Update docstring to document the new parameter

- **Pass headers parameter** in the search_messages tool implementation:
  - Update the client.search_messages call to include the headers parameter

- **Verify correlate_messages endpoint**:
  - Confirm `/tools/correlate_messages` endpoint exists and works correctly
  - Verify request/response schemas match the lib face

### 2. MCP Stdio Changes
- **Add headers parameter** to search_messages tool in `src/kafka_mcp/adapters/inbound/mcp_stdio.py`:
  - Add `headers: dict[str, str] | None = None` parameter to the function signature
  - Pass the headers parameter to client.search_messages call
  - Update docstring to document the new parameter

- **Add correlate_messages tool** in `src/kafka_mcp/adapters/inbound/mcp_stdio.py`:
  - Register new tool with appropriate signature and description
  - Implement the tool function that delegates to client.correlate_messages
  - Ensure proper serialization of results
  - Add readOnlyHint annotation

### 3. Integration Testing
- **Update integration tests** to cover cross-topic scenarios:
  - Add test for multi-topic search with headers filtering
  - Add test for correlation engine with cross-topic following
  - Ensure tests run against real testcontainers broker

### 4. Verification
- **Run existing test suite** to ensure no regressions:
  - `pytest tests/ --tb=short` should show zero failures
  - Total test count should remain ≥ 323

- **Run new tests** to verify v1.2 functionality:
  - `pytest tests/ -k "mcp" -v` for MCP adapter tests
  - `pytest tests/ -k "rest" -v` for REST API tests
  - `pytest tests/ -k "cli" -v` for CLI tests
  - `pytest -m integration -k "multi_topic or correlate" -v` for integration tests

## Implementation Approach

### REST API Implementation
1. Add headers field to SearchMessagesRequest Pydantic model
2. Update search_messages tool implementation to pass headers parameter
3. Verify correlate_messages endpoint is correctly implemented

### MCP Stdio Implementation
1. Add headers parameter to existing search_messages tool
2. Register new correlate_messages tool with proper signature
3. Ensure both tools use streamable-HTTP transport for large result sets

### Testing Strategy
1. Start with unit tests to verify individual components
2. Run integration tests against testcontainers broker
3. Verify cross-face consistency with equivalent inputs
4. Confirm no regressions in existing functionality

## Dependencies
- Depends on Phase 9 (Correlation Engine)
- Builds on existing MCP, REST, and CLI frameworks
- Requires working testcontainers integration test environment

## Backward Compatibility
- All new parameters are optional with proper defaults
- Existing functionality continues to work unchanged
- No breaking changes to public APIs

## Performance Considerations
- Maintain efficient serialization for large result sets
- Ensure proper error handling and validation
- Preserve existing performance characteristics

## Verification
- All existing tests pass (≥ 323 tests)
- New MCP tests pass with headers and correlation tools
- New REST tests pass with headers and correlation endpoints
- New CLI tests pass with headers and correlation commands
- Integration tests cover cross-topic scenarios
- Zero regressions in functionality