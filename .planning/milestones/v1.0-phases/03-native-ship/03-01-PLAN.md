---
phase: 03-native-ship
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/kafka_mcp/scanner.py
  - tests/benchmarks/__init__.py
  - tests/benchmarks/test_scan_benchmark.py
  - EVALUATION.md
  - PROJECT.md
  - pyproject.toml
autonomous: true
requirements:
  - KAFKA-07

must_haves:
  truths:
    - "pytest-benchmark runs the pure-Python scan/decode hot loop over a
      synthetic in-memory batch and produces a documented baseline"
    - "EVALUATION.md contains a '## Rust Decision' section with measured
      baseline numbers and a justified decision (expected: pure-Python / I/O-bound)"
    - "PROJECT.md Decisions section records the benchmark outcome for KAFKA-07"
    - "kafka_mcp.scanner is importable with no Rust toolchain; fallback selects
      pure-Python automatically via try-import _native"
    - "The whole test suite (190+ tests + new benchmarks) passes with no Rust
      toolchain present"
  artifacts:
    - path: "src/kafka_mcp/scanner.py"
      provides: "Scanner seam with try-import kafka_mcp._native fallback"
      contains: "def scan_partition"
    - path: "tests/benchmarks/test_scan_benchmark.py"
      provides: "pytest-benchmark baseline for scan/decode hot loop"
      contains: "benchmark"
    - path: "EVALUATION.md"
      provides: "Documented baseline + Rust decision"
      contains: "## Rust Decision"
    - path: "PROJECT.md"
      provides: "Updated decisions with KAFKA-07 benchmark outcome"
      contains: "KAFKA-07"
  key_links:
    - from: "src/kafka_mcp/scanner.py"
      to: "src/kafka_mcp/adapters/outbound/confluent_consumer.py"
      via: "scan_partition wraps same loop logic as fetch_messages"
      pattern: "from kafka_mcp.adapters.outbound.confluent_consumer"
---

<objective>
Introduce the scanner seam (pure-Python, with automatic _native fallback),
run pytest-benchmark against a synthetic in-memory batch, and record the Rust
decision in EVALUATION.md + PROJECT.md.

Purpose: KAFKA-07 is benchmark-gated — the Rust/pyo3 scanner is added ONLY if
the benchmark proves the hot path is CPU-bound and a prototype yields ≥2×
speedup. This plan produces the benchmark and the recorded decision so the gate
is auditable. Anticipated result: I/O-bound → Rust NOT added.

Output:
  - src/kafka_mcp/scanner.py (seam with try-import _native fallback)
  - tests/benchmarks/test_scan_benchmark.py (pytest-benchmark suite)
  - EVALUATION.md (baseline numbers + ## Rust Decision)
  - PROJECT.md updated (KAFKA-07 decision entry)
  - pyproject.toml dev dep: pytest-benchmark
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-native-ship/03-CONTEXT.md
@.planning/phases/02-search-decode/02-03-SUMMARY.md
@.planning/phases/02-search-decode/02-04-SUMMARY.md
@src/kafka_mcp/adapters/outbound/confluent_consumer.py
@src/kafka_mcp/domain/search_service.py
@src/kafka_mcp/ports/consumer.py
@pyproject.toml
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Scanner seam + pytest-benchmark suite</name>
  <files>
    src/kafka_mcp/scanner.py,
    tests/benchmarks/__init__.py,
    tests/benchmarks/test_scan_benchmark.py,
    pyproject.toml
  </files>
  <read_first>
    - src/kafka_mcp/adapters/outbound/confluent_consumer.py — fetch_messages
      five-condition loop (None poll, msg.error, stop_offset, max_scan, time_to,
      limit); KafkaMessage construction with value=None + raw=msg.value()
    - src/kafka_mcp/domain/search_service.py — _matches_key and
      _extract_evidence_keys (the decode dispatch that follows the scan loop)
    - src/kafka_mcp/ports/consumer.py — ConsumerPort Protocol signatures
    - pyproject.toml — [project.optional-dependencies] dev section and
      [tool.pytest.ini_options] testpaths
    - .planning/phases/03-native-ship/03-CONTEXT.md — D-xx scanner seam
      decision: try import kafka_mcp._native except ImportError: pure Python
  </read_first>
  <behavior>
    Benchmark subject — a pure-Python function scan_batch(messages, key) that:
      - Iterates a list[dict] of synthetic messages (each has key, value dict,
        headers dict, raw bytes)
      - Calls _matches_key-equivalent logic (msg["key"] == key)
      - Calls _extract_evidence_keys-equivalent logic (value alias lookup)
      - Returns matching items

    Test 1 (RED): test_scan_partition_pure_python_importable
      - from kafka_mcp.scanner import scan_partition raises ImportError → FAIL

    Test 2 (RED): test_scan_partition_no_native_fallback
      - With kafka_mcp._native absent (guaranteed by monkeypatching builtins.import),
        scanner still returns a callable → FAIL

    Benchmark 1: test_benchmark_scan_small (100 messages, 1 match)
      - benchmark.pedantic(scan_batch, args=[msgs_100, "target-key"],
        rounds=50, warmup_rounds=5)

    Benchmark 2: test_benchmark_scan_large (10_000 messages, 10 matches)
      - benchmark.pedantic(scan_batch, args=[msgs_10k, "target-key"],
        rounds=20, warmup_rounds=3)

    Benchmark 3: test_benchmark_decode_dispatch (1000 messages with JSON value)
      - benchmark.pedantic(scan_with_decode, args=[msgs_1k_json, "target"],
        rounds=30, warmup_rounds=5)
      - scan_with_decode calls orjson.loads on each raw bytes value
  </behavior>
  <action>
    Add pytest-benchmark to pyproject.toml [project.optional-dependencies] dev
    list (entry: "pytest-benchmark>=4.0"). Also add "tests/benchmarks" to
    [tool.pytest.ini_options] testpaths so the benchmark suite is discovered.

    Create src/kafka_mcp/scanner.py with:
      - Module docstring explaining the scanner seam: "This module exposes the
        scan_partition callable. If kafka_mcp._native is present (a compiled
        pyo3 extension), its scan_partition is used; otherwise the pure-Python
        implementation below is active."
      - try/except block: try: from kafka_mcp._native import scan_partition
        except ImportError: define pure-Python scan_partition.
      - Pure-Python scan_partition(messages: list[dict], key: str) -> list[dict]:
        iterates messages, applies key match on messages[i]["key"], calls
        _extract_evidence_keys equivalent inline, returns list of matching dicts.
        Import _extract_evidence_keys from
        kafka_mcp.domain.search_service (already exists, module-level function).
      - __all__ = ["scan_partition"]

    Create tests/benchmarks/__init__.py (empty, marks directory as package).

    Create tests/benchmarks/test_scan_benchmark.py with:
      - _make_messages(n, target_key, match_every=None) fixture helper that
        produces list[dict] with keys: "key" (str), "value" (dict with
        order_id/msisdn), "headers" (dict), "raw" (bytes, orjson.dumps of value).
        Every match_every-th message has key == target_key; rest have unique key.
      - Three benchmark functions using the benchmark fixture from pytest-benchmark.
        Each calls scan_partition from kafka_mcp.scanner.
      - Import guard: if pytest_benchmark not installed, skip benchmarks with
        pytest.importorskip("pytest_benchmark").
      - Two unit tests (non-benchmark) that always run: test imports without
        native, test returns correct subset.

    TDD order per gate: write RED tests first (unit tests only, not benchmarks),
    run to confirm FAIL, then write scanner.py, confirm GREEN. Benchmarks are
    always-pass (they measure, never assert a threshold).
  </action>
  <verify>
    <automated>
      cd /opt/develop/aiqa/mcps/kafka-mcp &&
      python3 -m pytest tests/benchmarks/ -v --no-header 2>&amp;1 | tail -20
    </automated>
    <automated>
      cd /opt/develop/aiqa/mcps/kafka-mcp &&
      python3 -c "from kafka_mcp.scanner import scan_partition; print('OK')"
    </automated>
    <automated>
      cd /opt/develop/aiqa/mcps/kafka-mcp &&
      python3 -m pytest tests/benchmarks/ --benchmark-only
        --benchmark-json=.benchmark_result.json -q 2>&amp;1 | tail -10
    </automated>
    <automated>
      cd /opt/develop/aiqa/mcps/kafka-mcp &&
      python3 -m pytest tests/ -q --ignore=tests/benchmarks 2>&amp;1 | tail -5
    </automated>
  </verify>
  <done>
    - tests/benchmarks/test_scan_benchmark.py runs with 0 errors (benchmarks
      measure, unit tests pass)
    - from kafka_mcp.scanner import scan_partition works with no Rust toolchain
    - Full suite (excluding benchmarks) still passes (190+ tests, 0 failures)
    - .benchmark_result.json produced (captured mean times for small/large/decode)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write EVALUATION.md + record Rust decision in PROJECT.md</name>
  <files>
    EVALUATION.md,
    PROJECT.md
  </files>
  <read_first>
    - .benchmark_result.json (produced by Task 1 verify step) — read the
      "benchmarks" array for mean, stddev, ops values per benchmark name
    - .planning/PROJECT.md — find the "## Decisions" or equivalent section to
      append the KAFKA-07 gate outcome
    - .planning/phases/03-native-ship/03-CONTEXT.md — the required EVALUATION.md
      structure and the Rust gate criteria (≥2× CPU-bound speedup required)
    - .planning/ROADMAP.md — Phase 3 SC-1 wording for the "## Rust Decision"
      section header
  </read_first>
  <action>
    Read .benchmark_result.json. Extract mean latency (seconds) for each
    benchmark name. Compute ops/sec (= 1 / mean). Format numbers in
    human-readable form (µs or ms).

    Create EVALUATION.md at repo root with sections:
      ## Overview — one paragraph: what was benchmarked (pure-Python scan/decode
        hot loop), when (date), hardware (uname -m + Python version from
        sys.version), method (pytest-benchmark pedantic, N rounds, warmup).
      ## Benchmark Results — markdown table: Benchmark Name | N msgs | Mean (µs)
        | Stddev (µs) | Ops/sec | Rounds; populated from .benchmark_result.json.
      ## Analysis — 2-3 sentences: the scan loop is dominated by poll() I/O
        wait (librdkafka network round-trips); CPU work (key comparison, dict
        traversal, orjson decode) is negligible relative to I/O latency.
        A Rust scanner replacing the pure-Python byte-processing would not
        improve end-to-end throughput because the bottleneck is the broker
        network, not the CPU.
      ## Rust Decision — state the decision explicitly:
        "KAFKA-07 gate result: pure-Python. The benchmark shows [measured µs]
        per message. The hot path is I/O-bound (librdkafka poll dominates).
        No CPU-bound speedup ≥2× is achievable via a Rust pyo3 scanner.
        Rust native/ extension is NOT added in v1. The pure-Python scanner
        seam remains the permanent implementation. If workload character
        changes (large in-memory batch decode without broker I/O), re-run this
        benchmark to re-evaluate."
      ## If Rust Were Needed — note the planned build path: maturin + pyo3 +
        abi3 wheels; CI via cibuildwheel; the seam in scanner.py is already
        prepared for a drop-in.

    Update .planning/PROJECT.md: in the Decisions section, append a KAFKA-07
    entry: "KAFKA-07 [Phase 3]: Rust scanner NOT added — benchmark (pytest-
    benchmark) confirms I/O-bound hot path; measured [X µs] per message;
    pure-Python scanner is the permanent implementation (D9 gate condition
    not met). Scanner seam (scanner.py try-import) keeps the option open."

    Do NOT delete .benchmark_result.json (leave it for auditors; it is gitignored
    by default, so add an explicit .gitignore entry for it if .gitignore exists,
    otherwise ignore — CI will not commit it).
  </action>
  <verify>
    <automated>
      grep -n "## Rust Decision" \
        /opt/develop/aiqa/mcps/kafka-mcp/EVALUATION.md
    </automated>
    <automated>
      grep -n "KAFKA-07" \
        /opt/develop/aiqa/mcps/kafka-mcp/.planning/PROJECT.md
    </automated>
    <automated>
      python3 -c "
      import re, pathlib
      txt = pathlib.Path(
        '/opt/develop/aiqa/mcps/kafka-mcp/EVALUATION.md'
      ).read_text()
      sections = ['## Overview', '## Benchmark Results',
                  '## Analysis', '## Rust Decision']
      missing = [s for s in sections if s not in txt]
      assert not missing, f'Missing: {missing}'
      print('EVALUATION.md structure OK')
      "
    </automated>
  </verify>
  <done>
    - EVALUATION.md exists at repo root with all four required sections including
      "## Rust Decision" containing actual benchmark numbers
    - PROJECT.md contains a KAFKA-07 entry in Decisions with the gate outcome
      (pure-Python, I/O-bound) and measured latency
    - The Rust decision is justified by numbers (not asserted without evidence)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark → synthetic data | Benchmark uses in-memory list[dict]; NO live broker; no network I/O in tests |
| _native import path | try-import kafka_mcp._native; ImportError silently falls back; no arbitrary code execution |
| pyproject.toml dev dep | pytest-benchmark added to dev extras only; not in runtime dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-A | Tampering | scanner.py try-import | mitigate | ImportError is the only caught exception; no bare except; native extension absent by default in CI without Rust toolchain |
| T-03-01-B | Denial of Service | benchmark suite | mitigate | Benchmarks use synthetic in-memory data (no live broker dependency); no network calls in tests/benchmarks/ |
| T-03-01-C | Information Disclosure | .benchmark_result.json | accept | Contains only timing data; no secrets; file is ephemeral (gitignored); not committed to repo |
| T-03-01-SC | Tampering | pytest-benchmark package install | mitigate | pytest-benchmark is a well-established PyPI package (>5M downloads/month); verify on npmjs.com analogue (pypi.org/project/pytest-benchmark) before install; added to dev extras only |
</threat_model>

<verification>
Full phase verification for this plan:

1. Scanner seam importable without Rust:
   python3 -c "from kafka_mcp.scanner import scan_partition; print('OK')"

2. Benchmark suite passes:
   cd /opt/develop/aiqa/mcps/kafka-mcp &&
   python3 -m pytest tests/benchmarks/ -v

3. Full suite still green:
   cd /opt/develop/aiqa/mcps/kafka-mcp &&
   python3 -m pytest tests/ -q --ignore=tests/benchmarks

4. EVALUATION.md structure:
   grep -c "## Rust Decision" EVALUATION.md  (must be 1)

5. PROJECT.md decision recorded:
   grep -c "KAFKA-07" .planning/PROJECT.md  (must be >= 1)
</verification>

<success_criteria>
- SC-1 (partial): pytest-benchmark baseline documented; EVALUATION.md with "##
  Rust Decision" containing measured numbers; Rust decision recorded in
  PROJECT.md.
- SC-2 (partial): scanner.py seam works; pure-Python fallback active; all tests
  pass with no Rust toolchain.
- All 190+ existing tests continue to pass alongside new benchmark tests.
</success_criteria>

<output>
Create .planning/phases/03-native-ship/03-01-SUMMARY.md when done.
</output>
