---
phase: 11-mtls-transport-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_mtls_e2e.py
autonomous: true
requirements: [MTLS-02]
user_setup:
  - service: mtls-kafka-broker
    why: "MTLS-02 end-to-end verification needs a real SSL/mTLS Kafka broker with client cert material. The test is env-gated and SKIPS (never fails) when these are absent, so CI without a broker stays green."
    env_vars:
      - name: KAFKA_MCP_MTLS_TEST_BOOTSTRAP
        source: "A reachable SSL-listener broker address, e.g. the proven staging contour 10.35.158.66:9094"
      - name: KAFKA_MCP_SSL_CERTIFICATE_LOCATION
        source: "Path to the client certificate PEM (never committed)"
      - name: KAFKA_MCP_SSL_KEY_LOCATION
        source: "Path to the client private key PEM (never committed)"
      - name: KAFKA_MCP_SSL_CA_LOCATION
        source: "Path to the CA cert PEM verifying the broker"
      - name: KAFKA_MCP_SSL_KEY_PASSWORD
        source: "Client key passphrase (optional; omit if the key is unencrypted)"
must_haves:
  truths:
    - "An integration test connects the brick to a real SSL/mTLS broker and runs a read-only op (list_topics / describe_topic) successfully over the mTLS handshake"
    - "The test is env-gated: it SKIPS (does not fail) when mTLS broker + cert env vars are absent, so the default `pytest -m 'not integration'` run and CI without a broker stay green"
  artifacts:
    - path: "tests/integration/test_mtls_e2e.py"
      provides: "env-gated real-broker mTLS end-to-end read-only test"
      contains: "pytest.mark.integration"
  key_links:
    - from: "tests/integration/test_mtls_e2e.py"
      to: "src/kafka_mcp/adapters/inbound/lib.py"
      via: "builds KafkaClient from an mTLS KafkaMcpSettings and calls list_topics()/describe_topic() over the SSL handshake"
      pattern: "KafkaClient|list_topics"
---

<objective>
Add the genuinely-new MTLS-02 deliverable: an end-to-end integration test that
connects the brick to a REAL SSL/mTLS Kafka broker and performs a read-only
operation (list_topics and/or describe_topic) over the client-certificate TLS
handshake, proving the ssl.* wiring works on the wire (not just in the conf dict).

The mTLS config wiring already exists (verified structurally by plan 11-01); this
plan proves it end-to-end. Per D (Claude's discretion) the test is gated behind
an env-guarded marker mirroring the existing v1.1 Phase 6 real-broker E2E contour
(tests/integration/, `@pytest.mark.integration`, graceful skip when the broker /
certs are absent). A working staging mTLS contour exists and was proven this
session (broker 10.35.158.66:9094, stage certs, SECURITY_PROTOCOL=SSL, via
`kafka-mcp list-topics` / `describe-topic eord`), so the test reads the broker
address + cert paths from env and skips cleanly when they are not provided.

Purpose: satisfy MTLS-02 (real-broker end-to-end mTLS integration test,
env-gated/skipped when creds absent, runs a read-only op like list_topics /
describe_topic).
Output: tests/integration/test_mtls_e2e.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-mtls-transport-hardening/11-CONTEXT.md

# Reuse the v1.1 Phase 6 real-broker E2E contour pattern:
@tests/integration/conftest.py
# tests/integration/test_basic_ops.py :: TestListTopics / TestDescribeTopic
#   show the read-only ops to exercise (list_topics, describe_topic) and the
#   pytestmark = pytest.mark.integration convention.

# The wiring under test (already implemented):
@src/kafka_mcp/config.py
@src/kafka_mcp/adapters/inbound/lib.py
# ConfluentConsumerAdapter maps settings.ssl_* -> librdkafka ssl.* on both the
# Consumer and AdminClient conf (see plan 11-01). KafkaMcpSettings exposes the
# ssl_* fields via the KAFKA_MCP_SSL_* env vars.
</context>

<tasks>

<task type="auto">
  <name>Task 1: env-gated real-broker mTLS end-to-end read-only test (MTLS-02)</name>
  <files>tests/integration/test_mtls_e2e.py</files>
  <action>
    Create tests/integration/test_mtls_e2e.py with module-level
    `pytestmark = pytest.mark.integration` (so it runs only under
    `pytest -m integration`, matching the Phase 6 contour and keeping the default
    `-m 'not integration'` addopts run untouched).
    Add a module-level skip guard: read the mTLS broker + cert material from env —
    a broker address var (KAFKA_MCP_MTLS_TEST_BOOTSTRAP, or fall back to
    KAFKA_MCP_BOOTSTRAP_SERVERS) plus KAFKA_MCP_SSL_CERTIFICATE_LOCATION,
    KAFKA_MCP_SSL_KEY_LOCATION, KAFKA_MCP_SSL_CA_LOCATION (KAFKA_MCP_SSL_KEY_PASSWORD
    optional). Use `pytest.mark.skipif(...)` (or an early pytest.skip in a fixture)
    keyed on "broker address AND cert AND key AND ca all present", with a clear
    reason string like "mTLS broker + client cert env not provided — skipping
    MTLS-02 e2e". This guarantees the test SKIPS, never ERRORS/FAILS, when creds
    are absent.
    Build a KafkaMcpSettings with bootstrap_servers=<the mTLS broker>,
    security_protocol="SSL", and the four ssl_* fields populated from the env
    paths/passphrase (pass values explicitly to KafkaMcpSettings(...) rather than
    relying on ambient env, so the test is self-contained). Construct a KafkaClient
    from a ConfluentConsumerAdapter(settings) (registry may be a null/None registry
    — decode is not needed for list_topics/describe_topic), using it as a context
    manager so the consumer is closed.
    Assert a read-only op succeeds over mTLS:
    - `topics = client.list_topics()` returns a list (mTLS handshake succeeded);
    - optionally, if a describe-target topic is provided via env
      (KAFKA_MCP_MTLS_TEST_TOPIC), call `client.describe_topic(that_topic)` and
      assert it returns a TopicInfo with partition_count >= 1.
    Keep it read-only (no produce). No cert material is committed — everything
    comes from env paths (T-11-01). Do NOT stand up an SSL broker inline; rely on
    the env-provided real/staging mTLS contour per D.
  </action>
  <verify>
    <automated>uv run pytest -m integration tests/integration/test_mtls_e2e.py -v</automated>
  </verify>
  <done>tests/integration/test_mtls_e2e.py exists, is marked integration, and: (a) SKIPS cleanly with a descriptive reason when mTLS broker/cert env vars are absent (verified by the command above showing skipped, not failed); (b) when the env is provided, connects over mTLS and a read-only op (list_topics, optionally describe_topic) succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test env -> broker (mTLS) | client cert/key/CA + optional passphrase cross from env into the live TLS handshake |
| cert material on disk | PEM cert/key files referenced by path; must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure | client key / passphrase in test | mitigate | Cert/key/CA/passphrase all sourced from env paths — no material committed to the repo. Passphrase flows through SecretStr in KafkaMcpSettings and is never logged. |
| T-11-04 | Denial of Service | test hangs on unreachable broker | accept | Test is integration-marked and env-gated; when creds absent it skips. Broker round-trips use the adapter's fixed metadata timeout, so a misconfigured contour fails fast rather than hanging indefinitely. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs (uses the existing confluent-kafka / testcontainers deps); no legitimacy checkpoint required. |
</threat_model>

<verification>
- `uv run pytest -m integration tests/integration/test_mtls_e2e.py -v` with no mTLS env → test is SKIPPED (not failed), with a descriptive reason.
- With the staging mTLS contour env provided (broker + cert/key/CA paths) → the test connects over mTLS and list_topics()/describe_topic() succeeds.
- `uv run pytest -m 'not integration' tests/` remains unaffected (the new file is integration-marked).
</verification>

<success_criteria>
- MTLS-02: a real-broker mTLS integration test exists under tests/integration/, is `@pytest.mark.integration`, runs a read-only op (list_topics / describe_topic) over the client-cert TLS handshake, and skips gracefully (never fails) when the mTLS broker + cert env vars are absent.
- No cert material committed; no src/ changes; the default non-integration test run is unaffected.
</success_criteria>

<output>
Create `.planning/phases/11-mtls-transport-hardening/11-02-SUMMARY.md` when done
</output>
