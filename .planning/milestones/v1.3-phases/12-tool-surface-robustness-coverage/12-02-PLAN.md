---
phase: 12-tool-surface-robustness-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_tool_face_coverage.py
autonomous: true
requirements: [COV-01, COV-02]
must_haves:
  truths:
    - "consumer_group_lag is exercised on the stdio MCP face and returns delegated LagRecord data"
    - "consumer_group_lag is exercised on the HTTP MCP face and returns delegated LagRecord data"
    - "consumer_group_lag is exercised on the REST face via POST /tools/consumer_group_lag"
    - "correlate_messages is exercised on the stdio MCP face and delegates to the client"
    - "correlate_messages is exercised on the HTTP MCP face and delegates to the client"
    - "correlate_messages is exercised on the REST face via POST /tools/correlate_messages"
    - "correlate_messages round-trips a base64 raw payload and a timestamp through initial_results back to the client"
  artifacts:
    - path: "tests/test_tool_face_coverage.py"
      provides: "COV-01 consumer_group_lag + COV-02 correlate_messages face coverage across stdio/HTTP MCP/REST"
      min_lines: 80
  key_links:
    - from: "tests/test_tool_face_coverage.py"
      to: "src/kafka_mcp/adapters/inbound/mcp_stdio.py"
      via: "create_mcp_server(...).call_tool for consumer_group_lag and correlate_messages"
      pattern: "call_tool\\(.*consumer_group_lag|call_tool\\(.*correlate_messages"
    - from: "tests/test_tool_face_coverage.py"
      to: "src/kafka_mcp/adapters/inbound/rest_api.py"
      via: "TestClient POST /tools/consumer_group_lag and /tools/correlate_messages + _create_http_mcp_server call_tool"
      pattern: "tools/consumer_group_lag|tools/correlate_messages"
---

<objective>
Add automated face coverage for the two previously-uncovered tools:
consumer_group_lag (COV-01) and correlate_messages (COV-02), across each face
they are exposed on — stdio MCP, HTTP MCP, and REST — including the base64 raw +
timestamp round-trip for correlate_messages.

Purpose: COV-01 and COV-02 are HARDENING/coverage requirements — the tools
already exist and delegate to KafkaClient. correlate_messages currently has NO
inbound-face coverage in tests/test_inbound.py (MockKafkaClient there has no
correlate_messages method). This plan adds TEST-ONLY coverage; it MUST NOT modify
any src/ file.
Output: tests/test_tool_face_coverage.py — a new, self-contained test module.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/kafka_mcp/adapters/inbound/mcp_stdio.py
@src/kafka_mcp/adapters/inbound/rest_api.py
@tests/test_inbound.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: COV-01 — consumer_group_lag coverage across stdio MCP, HTTP MCP, and REST</name>
  <files>tests/test_tool_face_coverage.py</files>
  <action>
Create a new test module tests/test_tool_face_coverage.py (fresh file — do NOT
edit tests/test_inbound.py). Reuse the sample-data and fake-client patterns from
tests/test_inbound.py: a fake client whose consumer_group_lag(group, topics)
returns deterministic LagRecord list for a known group and an empty list
otherwise (mirror MockKafkaClient.consumer_group_lag in test_inbound.py; import
LagRecord from kafka_mcp.domain.models). You may import MockKafkaClient from
tests.test_inbound if the import resolves; otherwise inline an equivalent
minimal fake.

Implement COV-01 (per COV-01 requirement) exercising consumer_group_lag on each
face it is exposed on:
  - stdio MCP: create_mcp_server(client), asyncio.run(server.call_tool(
    "consumer_group_lag", {"group": "my-group"})) returns non-empty content;
    parse the first TextContent JSON and assert group/topic/lag fields delegate
    from the fake (source == "kafka", event_type == "consumer_lag"). Follow the
    call_tool result-shape handling already used in test_inbound.py
    (TestMcpConsumerGroupLag) — handle tuple vs list return shapes.
  - HTTP MCP: _create_http_mcp_server(client), same call_tool assertion, proving
    the tool is registered and delegates on the HTTP MCP face.
  - REST: fastapi.testclient.TestClient(create_app(client)), POST
    /tools/consumer_group_lag with {"group": "my-group"} returns 200 and a
    result list whose first record carries the delegated group/topic/lag; also
    assert the topics filter narrows the result (POST with
    {"group": "my-group", "topics": ["orders"]} returns only the orders record).

Keep assertions focused on request/response shape and delegation to the client.
Assertion-only: do not modify any src/ file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_tool_face_coverage.py -k "consumer_group_lag or lag" -q</automated>
  </verify>
  <done>
consumer_group_lag is exercised and passes on the stdio MCP, HTTP MCP, and REST
faces; the REST topics filter is asserted; delegated fields
(source/event_type/group/topic/lag) are verified. No src/ file modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: COV-02 — correlate_messages coverage across faces with base64 raw + timestamp round-trip</name>
  <files>tests/test_tool_face_coverage.py</files>
  <action>
In the same tests/test_tool_face_coverage.py module, add COV-02 coverage (per
COV-02 requirement) for correlate_messages. Note: the MockKafkaClient in
tests/test_inbound.py has NO correlate_messages method, so define a fake client
here that DOES. The fake's correlate_messages(initial_results, follow_topics,
**kwargs) must:
  - capture the received initial_results (so the test can assert the base64 raw +
    timestamp round-trip reconstructed real KafkaMessage objects), and
  - return a deterministic list[KafkaMessage] (reuse the _SAMPLE_MSG shape from
    test_inbound.py: build a KafkaMessage with raw bytes and a tz-aware
    timestamp_utc). Import KafkaMessage from kafka_mcp.domain.models.

Build an initial_results payload the way a real caller would send it over a face:
a list of message dicts where raw is a base64 string and timestamp_utc is an
ISO-8601 string (this matches the inverse-of-_serialize_message decoding the
adapters perform). Use base64.b64encode on known raw bytes and a known ISO-8601
timestamp so the round-trip is checkable.

Exercise correlate_messages on each face it is exposed on:
  - stdio MCP: create_mcp_server(client), asyncio.run(server.call_tool(
    "correlate_messages", {"initial_results_data": [<dict>],
    "follow_topics": ["payments"]})). Assert non-empty result and that the fake
    received a reconstructed KafkaMessage whose raw equals the original bytes
    (base64 decoded) and whose timestamp_utc is the tz-aware datetime parsed from
    the string — this is the base64 raw + timestamp round-trip.
  - HTTP MCP: _create_http_mcp_server(client), same call_tool with
    "initial_results_data" and "follow_topics"; assert delegation.
  - REST: TestClient(create_app(client)), POST /tools/correlate_messages with
    JSON body {"initial_results": [<dict>], "follow_topics": ["payments"]}
    (note the REST request field is initial_results, per CorrelateMessagesRequest,
    whereas the MCP tool param is initial_results_data). Assert 200 and a result
    list; assert the returned message dict's raw is a base64 string that decodes
    back to the sample raw bytes (proving the serialize round-trip on the way
    out).

Because two faces use param name initial_results_data and REST uses
initial_results, keep the payload dict construction shared but pass it under the
correct key per face. Assertion-only: do not modify any src/ file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_tool_face_coverage.py -k "correlate" -q</automated>
  </verify>
  <done>
correlate_messages is exercised and passes on the stdio MCP, HTTP MCP, and REST
faces; the fake client receives reconstructed KafkaMessage objects proving the
base64 raw + timestamp round-trip inbound, and the REST response raw decodes back
to the original bytes proving the round-trip outbound. No src/ file modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → src imports | Tests import and drive production adapters in-process via TestClient / call_tool; no real broker or network boundary is crossed. |
| face payload → adapter decode | Tests feed base64/ISO-8601 face payloads through the adapters' inverse-serialize path; this exercises the untrusted-input decode boundary with benign fixtures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | correlate_messages base64/timestamp decode path in adapters | mitigate | Tests assert the decode round-trip reconstructs exact bytes/timestamps, catching a regression that corrupts raw or drops tz-awareness. Assertion-only. |
| T-12-04 | Denial of Service | Fake correlate returns unbounded results | accept | Fake returns a fixed small list; adapters already clamp limit (tested elsewhere). Low risk in test scope. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies introduced; tests use existing pytest + installed fastapi/mcp. No install tasks, so no legitimacy gate needed. |
</threat_model>

<verification>
- `python -m pytest tests/test_tool_face_coverage.py -q` passes.
- `python -m pytest tests/ -k "consumer_group_lag or lag" -v` passes (SC-3 selector).
- `python -m pytest tests/ -k "correlate" -v` passes (SC-4 selector).
- `python -m pytest tests/ --tb=short` stays green (SC-5, no regressions).
- `git diff --name-only src/` shows no source changes.
</verification>

<success_criteria>
- COV-01: consumer_group_lag exercised on stdio MCP, HTTP MCP, and REST faces
  with delegation + topics-filter assertions.
- COV-02: correlate_messages exercised on stdio MCP, HTTP MCP, and REST faces
  with the base64 raw + timestamp round-trip verified both inbound and outbound.
- Full suite green; no src/ behavior modified.
</success_criteria>

<output>
Create `.planning/phases/12-tool-surface-robustness-coverage/12-02-SUMMARY.md` when done
</output>
